Enter the following command in the terminal to execute the program(make sure your directory is in /bin):

#Create a new game
./main.out -n {FILENAME}.txt

#Load a old game
./main.out -l {FILENAME}.txt
